---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

inputs:
- name: repo

outputs:
- name: output

run:
  path: /bin/sh
  args: ["-c", "repo/ci/tasks/hello-world/task.sh"]
